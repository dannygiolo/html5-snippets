- <html> snippet (different type based on project);
- header
- footer
- nav (base, complex, dropdown, multi dropdown, breadcrumbs, navigation);
- article
- media object (full, text only, thumbnails);
- search page/newslist (full, text only, thumbnails);
- browse app (a la nbc)
- form (left/top labels)
- search
- lists (definition, horizontal, vertical)
- data table
- tabs

